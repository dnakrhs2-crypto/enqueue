# Tally 0.1.8 검증 기록 (2026-09-13) — 이름 확정 "Tally(탤리)"

범위: 대표가 2026-09-13 제품 이름을 Tally(탤리)로 확정. "Recorder (가칭)" → Tally 이름 변경(4f43e8c) + 곰튀김.com 제품 페이지 + 리뷰 수정(6fb5e57, d4f56c7). 기능은 0.1.7과 같다.

## 바뀐 것
- ProductIdentity: 표시 이름 Tally, 패키지 이름 Tally(Tally.exe, Tally-Setup-0.1.8.exe), 사이트 폴더/URL tally, 소스 zip 이름 Tally-sources. **유지**: 설정 폴더 Recorder(설치 폴더·설정·WinSparkle 레지스트리 키), 프로젝트 확장자 .recorder, 파일 형식 Recorder.Project, 내부 ID, AppId, GitHub 저장소 dnakrhs2-crypto/recorder, 태그 접두사 recorder-v, appcast URL — 기존 설치·설정·프로젝트·자동 업데이트가 그대로 이어지도록.
- 설치기: [InstallDelete]로 옛 Recorder.exe·옛 이름 바로가기(시작 메뉴 그룹 `Recorder (가칭)`, 바탕화면 `Recorder (가칭).lnk`) 정리, [UninstallDelete] Recorder.exe. PrepareToInstall: Recorder.exe 삭제를 최대 30초 재시도(매핑된 실행 파일은 삭제 불가 → 삭제 성공 = 구버전 종료 확인), 실패 시 안내와 함께 설치 중단(WinSparkle이 Setup을 먼저 띄우고 그 뒤 앱 종료를 요청하는 순서 때문).
- 릴리스 도구: 스테이징은 CMake가 선언한 실행 파일만 복사(재사용 빌드 폴더의 옛 Recorder.exe 혼입 차단), 선언 EXE가 링크면 거부; validate_bundle은 payload에 앱 외 .exe가 있으면 거부(회귀 테스트 추가); 최신 릴리스 자산 정규식에 Tally 추가.
- 사이트: site/tally/(LiveMix 페이지 구조: 아이콘·한 줄 소개·기능 4개·1440×900 스크린샷·알 수 없는 게시자 안내·FFmpeg 고지·notices), Enqueue/LiveMix 페이지 상단 탭에 Tally, site/recorder/{index,notices,notes}.html은 /tally/로 리다이렉트.
- 문서: README·NOTICE 이름, 릴리스 노트 0.1.8.

## 스크린샷
실제 Tally 창(타임라인 탭, 데모 프로젝트)을 PrintWindow로 캡처. 캠1 프레임은 캡처보드에 가정용 카메라 영상이 들어와 있어 컬러바로 대체했고, 일시적인 저널 복구 안내 줄은 지웠다. UI 자체는 실물.

## 리뷰 (읽기 전용, 코덱스)
- 1차 `tally-review1-OUT.md`: "fix first" — P1 재사용 패키지 빌드 폴더의 옛 Recorder.exe가 payload에 섞임, P2 옛 바로가기 잔존, P2 실행 중인 0.1.7과 삭제 경쟁, P2 /recorder/notes.html 404.
- 2차 `tally-review2-OUT.md`: 3건 닫힘, 삭제 경쟁 P2 미해소(파킹은 종료 증명이 아님) + 신규 P3(선언 EXE 링크 검사) → d4f56c7에서 수정.
- 3차 `tally-review3-OUT.md`: "ship" — P2·P3 모두 닫힘, 추가 결함 없음. WinSparkle 흐름(종료 가능 검사 → Setup 실행 → 종료 요청)에서 순환 대기 없음, /SILENT에서도 중단 안내가 표시되고(종료 코드 7) 부분 설치·Tally 재실행 없음, 녹화 중이면 설치 중단이 올바른 결과.

## 검증 (전부 이 이름의 빌드)
- `RecorderTests --suite all` (RECORDER_TEST_NO_HARDWARE=1): 739 passed / 0 failed, 49개 스위트 (`tools/claude_harness/tests_018_tally1.log`; C++ 소스는 이름 변경 커밋 이후 변경 없음).
- 릴리스 도구 `python -B -m unittest discover -s tools/recorder/tests`: 33 tests OK (skipped 2) — 새 테스트 test_second_executable_in_payload_is_rejected 포함.
- 실장비 데모 `RecorderProbe demo --app Tally.exe`(캡처보드 GC311G2 + FlexASIO) 녹화 3회: PASS (정지→첫 재생 429/409/427 ms) — `recorder_validation/v016/demo-018-tally-record.json`.
- `release.py --package-only` 드라이런: 1차(6fb5e57) 기술검증 PASS·ISCC 컴파일 OK·payload 실행 파일 Tally.exe 하나; 2차(d4f56c7) 기술검증 PASS·수정된 PrepareToInstall을 포함한 ISCC 컴파일 OK·payload 실행 파일 Tally.exe 하나·Tally-Setup-0.1.8.exe 서명 완료 (out/release/recorder/0.1.8-1s3c31es).

## 남은 것 / 알아 둘 것
- GitHub 고정 링크 `releases/latest/download/Recorder-Setup.exe`는 0.1.8부터 `Tally-Setup.exe`로 바뀌어 옛 고정 링크는 끊긴다(앱은 appcast, 사이트는 버전별 자산 URL을 쓰므로 영향 없음).
- 옛 이름이 남는 내부 표기: 크래시 리포트 파일명 접두사 `Recorder-`, 내보내기 내부 오류 문구, Probe 사용법 문자열(사용자 화면 아님).
- 0.1.7 → 0.1.8 실제 제자리 업데이트는 실장비 PC에서 자동 업데이트로 확인 예정(0.1.6 리뷰 때부터 release gate "clean-PC-install-update" 미해결 항목).
- 0.1.6 리뷰 P3 `tools/CutEditStress.cpp` 숨김 트랙 행 계산(진단 도구)은 그대로.
