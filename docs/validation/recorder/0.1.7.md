# Recorder 0.1.7 검증 기록 (2026-09-12)

범위: 디자인 시안 13 "프리미어 다크" 적용(57a48f4) + 버전/릴리스 노트(56bb89b) + 디자인 리뷰 1차 수정(f71db08). 기능은 0.1.6과 같다.

## 리뷰
- 1차(읽기 전용, `design-13-review1-OUT.md`): "fix first" — P1 안내 행이 나중에 나타나면 0 px로 남음, P2 마커 이름 흰색 고정, P2 구간 강조가 룰러·클립 위에 없음, P2 트림 한계 노란 핸들 사라짐, P3 탭 비활성/hover, P3 하단바 매 갱신 repaint.
- 수정(f71db08): RecordView::update가 안내의 빈 상태 변화에도 resized() 재실행(noticeBounds 접근자), 마커 글자색 밝기 기준 복원, 구간 오버레이(accent .17, 룰러 포함 전체 높이 + 룰러 양끝 1 px)를 클립 위에 추가, leftLimit/rightLimit 노란 핸들 복원(둥근 모양 유지), 탭 45% 알파·hover/down, 하단바는 문자열·녹화 상태가 바뀔 때만 repaint·폭 측정.
- 2차(읽기 전용, `design-13-review2-OUT.md`): "ship" — 6건 모두 닫힘, 새 배포 차단 결함 없음. 요청문의 "타임라인 26 px 이동"은 계산 오류였고(녹화 탭은 카메라 높이가 26 px 줄어 하단 영역 유지, 타임라인 탭은 하단 영역 18 px 이동·축소) 실제 테스트는 그 구조에 맞춰 작성됨.

## 회귀 테스트 (전부 이 커밋의 빌드)
- `RecorderTests --suite all` (RECORDER_TEST_NO_HARDWARE=1): 739 passed / 0 failed, 49개 스위트 (`tools/claude_harness/tests_017_fix7.log`).
- 새 테스트: 안내 행 등장·해제(녹화·타임라인 탭, 960×640 고정), 흰색·노란색·어두운 마커 이름 대비, 전체 자산 클립의 노란 핸들과 trimIn 후 좌우 구분, 몸통 드래그 구간 선택 시 룰러·클립 픽셀 변화.
- 테스트 작성 중 잡은 내 실수: `UserSettings::physicalInputs`는 빈 `std::vector`라 `[0] = 0`이 범위 밖 쓰기(프로세스 크래시) — 벡터 대입으로 수정. 제품 코드 결함 아님.

## 실장비 데모 (캡처보드 GC311G2 + FlexASIO, `RecorderProbe demo`)
- 녹화 탭 3회: PASS (정지→첫 재생 400/385/426 ms) — `recorder_validation/v016/demo-017fix-record.json`
- 타임라인 덮어쓰기 3회: PASS (390/455/549 ms) — `demo-017fix-overwrite.json`

## 남은 것
- 0.1.6 리뷰의 P3: `tools/CutEditStress.cpp` 행 계산이 숨김 트랙을 반영하지 않음(진단 도구, 제품 동작 아님).
- 미설치 글꼴(Cascadia Mono → Consolas 분기)·다른 DPI의 실제 렌더링은 미검증.
