# Tally 0.1.12 검증 기록 (2026-09-13)

## 범위
- 마커 3종(목록·위치 칸의 시간 표시, `mm:ss 이름` 텍스트 내보내기, 색 견본 10색) — 코덱스 write 세션(fix-markers, 9cacd80) + 파서 수정, b95ccc3으로 integrate-016에 병합. 1차 리뷰 `recorder_markers_review_OUT.txt`: fix first(P2 1건 = 내보내기 교체 실패 시 기존 파일 손실 가능) → `MarkerPanel::landExport`: 대상이 없으면 이동, 있으면 백업 이름(미존재 보장)과 함께 `ReplaceFileW`; 실패 시 이전 파일은 자기 이름 또는 백업 이름으로 남고 되돌리며, 되돌리기까지 실패하면 백업 경로를 사용자에게 알림(테스트: 잡힌 대상·1177형 실패·되돌리기 차단·첫 저장).
- RecorderTests 간헐 크래시("메모리는 read될 수 없습니다" 창) 원인 제거 + 무인 실행 크래시 가드.
- Ctrl+S 프로젝트 저장 단축키(기본값, 옛 설정 파일 마이그레이션).
- 녹화 버튼→녹화 시작 지연 실측·축소(시작 여유 250 → 100 ms) + take.json 구간별 시간 기록.

## 크래시 원인 추적
- 이력: 전체 스위트 실행 약 14회 중 2회(9/12 int5 = WER 덤프 `RecorderTests.exe.23308.dmp`, 9/13 마커 병합 빌드) 같은 지점(materials-alignment "Two cameras linked cut…")에서 읽기 AV. 단독 스위트·부하(busy loop 8개) 반복 5회·디버그 힙(cdb -hd) 3회·일반 1회 = 모두 통과 → 힙 배치 의존 버그로 판단.
- 도구: WinDbg(winget `Microsoft.WinDbg`, `cdbX64.exe`)로 옛 덤프 분석(심볼 없음, 메인 스레드 RecorderTests+0x5f7b51 읽기 AV), Release에 `/Zi` + RecorderTests `/DEBUG`로 PDB 생성, `RECORDER_ASAN` 옵션으로 별도 빌드(`build/asan`).
- ASan 1회차: `ExportPublication::commit` heap-use-after-free — `juce::JSON::toString(...).toUTF8()`의 CharPointer가 임시 String 소멸 후 사용(READ 3901 bytes). 수정: String을 지역 변수로 유지. 같은 형태(`= ….toUTF8();` 등) 전수 검색: 나머지 2곳은 이름 있는 변수라 안전.
- 크래시 가드(`tests/TestCrashGuard.cpp`): SetErrorMode(SEM_NOGPFAULTERRORBOX 등)·abort/terminate/invalid-parameter 핸들러·SetUnhandledExceptionFilter → 실행 중 테스트 이름과 예외를 stderr/`<exe>\crash\*.txt`에 기록, 미니덤프 작성, 예외 코드로 종료. 검증: PowerShell에서 `--suite inject-access-violation` → 1.0 s 내 exit 0xC0000005, 데스크톱 새 창 0(창 감시), 덤프가 `TestCrashGuard.cpp:85`로 심볼 해석. 회귀 테스트가 shortcut-exceptions에 있음. 앱 `CrashHandler::install`에도 같은 오류 모드 적용.
- `--suite a,b,c`(순서 실행, 이름 선검증) 추가.

## 녹화 시작 지연 (실장비: 캠1 1080p60 MJPEG 기기 타임스탬프, ASIO 48 kHz, NVMe)
| 상황 | 축소 전 | 축소 후 |
|---|---|---|
| 녹화 탭 첫 테이크 | 936 ms (준비 120 + 대기 551 + 여유 264) | 752 (125 + 509 + 117) |
| 녹화 탭 이후 | 383 / 365 (준비 ~110, NVENC ~90, 여유 ~260) | 231 / 223 (여유 ~115) |
| 타임라인 덮어쓰기 첫 | 1091 (122 + 703 + 265) | 772 (120 + 535 + 117) |
| 타임라인 덮어쓰기 이후 | 424 / 440 (여유 ~314) | 277 / 272 (여유 ~157) |
- 여유는 오디오 워커의 TakeStarted 저널 내구 기록을 기다리는 시간(missedStart 안전장치 유지). 첫 테이크의 대기는 장치 시작 직후 ASIO 클록 유효 판정 대기. 데모 6회 모두 PASS(stop→clip ≤ 90 ms, 첫 재생 ≤ 504 ms), 타임라인 배치 OK.
- 대표 보고 "다른 PC(NV12 30fps)에서 3초": 미재현. 추정 = 기기 타임스탬프 없는 카메라의 도착 시각 클록 맞추기(readyWait). take.json의 prepareWorkMs/readyWaitMs/armedToRecordingMs로 확인 예정.

## 테스트
- 스위트 단독: shortcut-exceptions 29/0, timeline-ux 66/0, cut-edit-stability 13/0, lifecycle 18/0, materials-alignment 19/0 ×3, export-lifecycle 6/0 ×3.
- 전체 스위트(Release): 2차 수정 전 `tests_0112_final2_all.log` 760/0; 2차 수정 후 `tests_0112_final3_all.log` 760 passed / 0 failed, all suites passed(크래시 가드 보고 없음). 2차 수정 후 단독: shortcut-exceptions 29/0, timeline-ux 66/0, take-lifecycle 37/0.
- 2차 수정 후 실장비 데모 녹화탭 3/3(appVersion 0.1.12): 버튼→녹화 780/234/227 ms(onClick 전 시각 기준), durableWriteMs 12.5/7.7/7.5 → startLeadMs 100.0, stop→clip ≤ 24 ms, 첫 재생 ≤ 486 ms.
- ASan 전체 스위트 재실행(UAF 수정 후, 2차 리뷰 수정 전 바이너리, `tests_0112_asan_1.log`): 760 passed / 0 failed, all suites passed, 검출 0 (32분). 2차 수정 후 변경 스위트 ASan 재실행: PENDING_ASAN2
- 한 차례 실패 기록: 최종 직전 전체 실행에서 TimelineRecordingTests.h:241(옛 여유 2000샘플 하드코딩 → Fs/10 규칙으로 수정)과 cut-edit-stability "cut seam … exact frames"(ASan 빌드와 동시 실행 중 부하, 단독 13/13).

## 리뷰
- 2차(아스트라 read-only, `brief_recorder_0112_review2.txt` → `recorder_0112_review2_OUT.txt`, 13분): **fix first** — P2 2건, P3 2건.
  - P2 마커 내보내기 폴백 `copyFileTo`가 대상을 지운 뒤 복사(복사 실패 시 원본 손실) → `ReplaceFileW`를 백업 이름과 함께 직접 호출(실패 시 원본은 자기 이름 또는 백업 이름으로 남고, 이름이 바뀌었으면 되돌림), 성공 시 백업 삭제. 테스트: 임시·백업 잔재 없음(실패·성공 둘 다).
  - P2 저장 단축키 후보 3개가 모두 점유된 옛 설정은 전체 로드 실패(장치·카메라 설정까지 기본값) → 후보 6개(ctrl+S, ctrl+shift+S, ctrl+alt+S, ctrl+shift+alt+S, F12, ctrl+F12; 다른 명령 5개보다 많아 항상 하나는 비어 있음, static_assert). 테스트: S 후보 3개 점유 파일 → ctrl+shift+alt+S로 로드.
  - P3 버튼→녹화 측정이 onClick 안의 동기 작업(첫 테이크 체크포인트 저장 포함)을 빼먹음 → 데모는 onClick 전에 시각 기록, prepare()는 진입 시각을 prepareQpc로 사용.
  - P3 크래시 보고의 테스트 이름이 포인터(임시 문자열 이름 존재) → 256바이트 복사 버퍼(`latestTestName`, [255]는 항상 NUL).
  - 참고 지적 반영: 100 ms 여유의 HDD 안전성 미검증 → 여유를 준비 중 실측한 take.json 내구 쓰기 시간의 4배까지 늘리고(최소 100 ms/4버퍼, 최대 이전 값 250 ms) `startLeadSamples()`로 노출, take.json에 durableWriteMs/startLeadMs 기록; 릴리스 노트 문구(다음 후보·최소 0.10초) 수정. 미반영(수용): 설정 창 657 px 미만에서 초기화 버튼 잘림 가능, 크래시 가드가 힙 손상 시 교착 가능(테스트 실행기 한정), prepareDoneQpc가 tick 관측 시각.
- 3차(`brief_recorder_0112_review3.txt` → `recorder_0112_review3_OUT.txt`, 9분): **fix first** — P2 2건, P3 1건; 2차 P2 저장키·측정 시작점·디스크 실측 계수·UAF·listening·Ctrl+S·빌드는 '해결/타당'.
  - P2 되돌리기 실패 시 백업 위치를 사용자에게 알리지 않음(1177 후 restore 실패 경로 미검증, 백업 이름 미존재 미확인) → `MarkerPanel::landExport(target, temporary, swap)`로 분리: 백업 이름 `getNonexistentChildFile`, 되돌리기 결과 확인, 실패 시 백업 전체 경로를 담은 메시지. 테스트: 주입 swap으로 1177형 되돌림, 백업을 공유 없이 잡아 되돌리기 차단(경로 안내), 단순 실패, 첫 저장.
  - P2 `min(Fs/4, …)` 상한이 큰 버퍼(예 48k·16384)의 하한을 잘라 missedStart 위험 → `startLeadFor` = max(2버퍼, clamp(4×실측, Fs/10, Fs/4)); 순수 함수 테스트 5건(4800/7680/12000/32768/800). 릴리스 노트에 큰 버퍼 문구.
  - P3 이름 버퍼 데이터 경쟁(비원자 snprintf) → `std::array<std::atomic<char>,256>` 바이트별 relaxed, 보고 진입 시 1회 복사.
  - 3차 수정 후: Release timeline-ux 67/0·shortcut-exceptions 29/0·take-lifecycle 38/0, 전체 762 passed / 0 failed(`tests_0112_final5_all.log`); ASan timeline-ux·shortcut-exceptions·take-lifecycle 134/0 검출 0(`tests_0112_asan-fix3b_1.log`). 새 landing 테스트의 '되돌리기 차단'은 JUCE FileInputStream(READ|WRITE|DELETE 공유)으로는 막히지 않아 공유 없는 CreateFileW 핸들로 잡음.
- 최종 빌드 실장비 데모(녹화탭 2/2, appVersion 0.1.12, 버퍼 480): 버튼→녹화 781/226 ms(첫 테이크 ASIO 클록 대기 547 포함), durableWriteMs 6.5/7.1 → startLeadMs 100.0, stop→clip ≤ 18 ms, 첫 재생 ≤ 403 ms.
- 4차(`brief_recorder_0112_review4.txt` → `recorder_0112_review4_OUT.txt`): **ship** — 3차 P2 2건·P3 1건 해결 확인, 추가 배포 차단 결함 없음. 비차단 문서 지적: 위 '762'는 `PASS` 출력 행 수이고, 개별 PASS를 찍지 않는 AsioStamp 10·NativePcm 12를 더한 스위트 요약 합계는 784 passed / 0 failed(집계 기준 명시).
- 리뷰 전문: `0112-review1-markers-OUT.md`(마커 1차), `0112-review2-OUT.md`, `0112-review3-OUT.md`, `0112-review4-OUT.md`.
