# Tally 0.1.11 검증 기록 (2026-09-13) — 프로젝트 없이도 업데이트가 막히던 문제

증상(대표): 프로젝트를 만들지 않은 상태에서 업데이트 버튼 → "녹화·더빙·내보내기·복구와 저장이 끝난 뒤 업데이트를 다시 시도하세요".
원인: `RecorderDocument`는 생성 시 `dirty = true`, `newProject()`도 dirty 유지 → 앱 시작 시의 빈 기본 프로젝트(파일 없음)가 `publishLifecycle()`에서 `RecorderLifecycle::unsaved`로 발행 → `canShutdown()` false → 모든 활동을 나열하는 generic 문구.
수정(9da4c70): `RecorderDocument::hasUnsavedWork()` = dirty && (파일 있음 || activeTimelineEnd > 0) — 종료 경로와 같은 규칙; `MainComponent::updateBlockedText()`가 실제 켜진 플래그만 나열(녹화/더빙/내보내기/복구/테이크 마무리/저장/장치 연결/종료/오디오 장치 사용), unsaved만이면 "프로젝트에 저장하지 않은 변경이 있습니다. 저장한 뒤 업데이트하세요."
리뷰: 코덱스 읽기 전용 `gate-review1-OUT.md` "ship" — 종료 규칙과 일치, 자동 업데이트(WinSparkle 콜백·조용한 검사) 경로에서도 미저장 미디어 보호 유지, exclusive 검사 불변, UTF-8 경계 OK.
검증: RecorderTests --suite all 749 passed / 0 failed(신규 1: 빈 기본 프로젝트 허용 → 저장된 프로젝트 + 마커 편집 차단(저장 사유만) → 저장 후 허용, 문구 조합; `tools/claude_harness/tests_0111_gate.log`). 녹화·불러오기는 프로젝트 폴더가 필요해 "파일 없는 프로젝트에 미디어"는 앱에서 만들 수 없음(그 분기는 종료 규칙 대칭용).
