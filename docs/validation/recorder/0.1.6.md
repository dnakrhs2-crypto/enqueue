# Recorder 0.1.6 검증 (2026-09-12)

대표 요구 22건(원문 목록 + 추가 4건: 안 쓰는 트랙 숨김, 드래그 구간 삭제, 오디오 불러온 뒤 맨 뒤에서만 녹음됨, 들으면서 녹화). 기준 23a99e0(0.1.5) → 브랜치 `integrate-016`.

## 작업 구조
- 사전 정리 4c71826(제목·확대 버튼·불러오기 문구·오류 문구 UTF-8, 일시정지/방금 테이크 재생/일반·더빙 버튼 제거, 마커 훅).
- 코덱스(gpt-6-astra) 병렬 세션 4개 + 후속 1개, 각 세션 보고서 = `fix-016-{camera,app,record,timeline,playback}-OUT.md`.
- 내가 직접: 안 쓰는 트랙 숨김·클립 아래쪽 드래그 구간 선택(3b2a539), 병합 회귀 수정(재생 latest 끝 검사 c38a9fb, 테스트 fixture), 데모 자동화 `--demo-timeline`(d7d8c8d).

## 단위/통합 테스트
- 병합본 전체 `RecorderTests --suite all`: **all suites passed**(요약 합계 723+, 실패 0). 로그 `tools/claude_harness/tests_016_int9.log`.
- 세션별 기준선/최종: camera 695→+11, app 685→695(+11), record 685→707(+22), timeline 695(+10), playback 707→723(+16).
- 컷 이음새 실시간 테스트 2건은 코덱스 빌드 부하 중 1회 실패(int1) 후 조용한 상태에서 전부 통과(int7~int9).

## 실장치(캡처보드 GC311G2 MJPEG 1080p60 + FlexASIO 48k/480) 데모
`RecorderProbe demo --app Recorder.exe --devices r01/devices.json --asio-device 0 --iterations 3 [--timeline gap|overwrite]`, 결과 `recorder_validation/v016/demo-int9-*.json`.
- 녹화 탭 3회: PASS. 정지→클립 p95 19.6ms, 정지→첫 재생 p95 429ms.
- 타임라인 빈 구간 3회: PASS(int10). 배치 = 끝+5초 정확(720480/1440960), 앞 클립 불변. (int9는 데모 도구의 검사 조건 실수로 FAIL 표기, 제품 동작은 동일.)
- 타임라인 덮어쓰기 3회: PASS. 배치 = 끝−3초 정확(336480/675840), 이전 캠1 클립 끝이 새 배치 위치로 잘림, 테이크 보고에 정렬 정보(alignment) 기록 = 들으면서 녹화 경로 동작.
- 회귀 발견·수정: 병합본 첫 데모에서 "방금 테이크 재생"이 정지 직후 끝 검사에 걸려 재생되지 않음(중간녹화 세션의 끝 검사) → latest 요청 예외(c38a9fb) 후 PASS.

## 미측정 (대표 실물 필요)
- 들으면서 녹화의 물리 정렬(±5ms 오디오, ±1프레임 영상): 합성 장치 지연으로만 검증. 실측 절차는 `fix-016-playback-OUT.md`(루프백 펄스, 동기 LED).
- 실제 스트림캠·C920 2캠, 벤더 ASIO 드라이버, 스튜디오 PC 설치/업데이트.
